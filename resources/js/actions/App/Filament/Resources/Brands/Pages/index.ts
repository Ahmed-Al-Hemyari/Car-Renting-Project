import ListBrands from './ListBrands'
import CreateBrand from './CreateBrand'
import ViewBrand from './ViewBrand'
import EditBrand from './EditBrand'
const Pages = {
    ListBrands: Object.assign(ListBrands, ListBrands),
CreateBrand: Object.assign(CreateBrand, CreateBrand),
ViewBrand: Object.assign(ViewBrand, ViewBrand),
EditBrand: Object.assign(EditBrand, EditBrand),
}

export default Pages