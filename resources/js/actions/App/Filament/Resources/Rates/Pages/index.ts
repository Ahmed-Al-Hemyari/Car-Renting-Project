import ListRates from './ListRates'
import CreateRate from './CreateRate'
import EditRate from './EditRate'
const Pages = {
    ListRates: Object.assign(ListRates, ListRates),
CreateRate: Object.assign(CreateRate, CreateRate),
EditRate: Object.assign(EditRate, EditRate),
}

export default Pages